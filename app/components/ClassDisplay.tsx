import { View, Text } from "react-native"
import React from "react"

type ClassDisplayProps = {
  className: string
  classAbout: string
  classDuration: string
}

const ClassDisplay = () => {
  return (
    <View>
      <Text>ClassDisplay</Text>
    </View>
  )
}

export default ClassDisplay
