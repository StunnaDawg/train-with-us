// import React from "react"

// type EmojiProp = {
//   className: string
//   label: string
//   symbol: number
// }

// const Emoji = React.memo(({ className, label, symbol }: EmojiProp) => (
//   <span className={className} role="img" aria-label={label}>
//     {String.fromCodePoint(symbol)}
//   </span>
// ))

// export default Emoji
